===============
python-sotools
===============

Collection of tools to debug and diagnose ELF objects.

In order to print the paths that are attempted by sotools/linker.py's resolve when using ldd.py, add the `--print-tries` flag at the end of the command line.
